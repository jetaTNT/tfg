<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_libro"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/card_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp">

        <!-- Se reemplaza ic_book inexistente por un drawable del sistema -->
        <ImageView
            android:id="@+id/ivPortada"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@android:drawable/ic_menu_agenda"
            android:contentDescription="@string/portada_libro" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tvTituloLibro"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="Título del libro"
            style="?attr/textAppearanceHeadline6"
            android:textColor="@color/primary_text"/>

        <!-- Se reemplaza ic_chevron_right por un drawable del sistema -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="8dp"
            android:src="@android:drawable/ic_media_next"
            android:contentDescription="@string/ir_adelante" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
