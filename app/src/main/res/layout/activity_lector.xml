<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E3F2FD">

    <!-- Contador de puntos - ID CORREGIDO -->
    <TextView
        android:id="@+id/tvPuntos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⭐ Puntos: 0"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#FF6F00"
        android:fontFamily="@font/baloo"
        android:background="#FFFFFF"
        android:padding="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp" />

    <!-- Contenido de la página - ID CORREGIDO a tvPagina -->
    <TextView
        android:id="@+id/tvPagina"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="24dp"
        android:text="Aquí va el texto del libro"
        android:textSize="20sp"
        android:textColor="#1A237E"
        android:fontFamily="@font/baloo"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvPuntos"
        app:layout_constraintBottom_toTopOf="@id/btnSiguiente"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

    <Button
        android:id="@+id/btnSiguiente"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Siguiente ➡️"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:fontFamily="@font/baloo"
        android:backgroundTint="#FFCC80"
        android:textColor="#4E342E"
        android:padding="16dp"
        android:minWidth="160dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="32dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
